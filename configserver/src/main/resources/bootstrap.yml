spring:
    application:
        name: config-server
    profiles:
        active: native

    cloud:
      config:
          server:
              bootstrap: true
              native:
                  search-locations: file://classpath:/config

server:
    port: 8071
